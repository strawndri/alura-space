{
    "builds": [{
        "src": "alura-space/wsgi.py",
        "use": "@ardnt/vercel-python-wsgi",
        "config": { "maxLambdaSize": "15mb" }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "alura-space/wsgi.py"
        }
    ]
}